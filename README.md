# doc

## virtualenv
```
mkdir workspace
cd workspace/
pip install virtualenv
virtualenv interview_env --python=python2.7
source interview_env/bin/activate
pip install -r ../python-interview-prep/interview_requirements.txt
```